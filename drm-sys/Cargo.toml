[package]
name = "drm-sys"
version = "0.1.2"
edition.workspace = true
license.workspace = true

[features]
use_bindgen = []

[build-dependencies]
bindgen = { workspace = true }

[dependencies]
libc = { workspace = true }
